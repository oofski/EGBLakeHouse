name = "ebg-lakehouse"
pages_build_output_dir = "public"
compatibility_date = "2024-11-01"

# D1 + R2 bindings are configured in the Cloudflare dashboard
# (Pages project → Settings → Bindings) so no IDs are needed here.
# Variable names the Functions expect: DB (D1) and FILES (R2).
